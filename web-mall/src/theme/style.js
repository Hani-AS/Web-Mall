import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F0CAC0",
      contrastText: "#442C2E",
    },
    secondary: {
      main: "#442C2E",
      contrastText: "#fff",
    },
  },
});
